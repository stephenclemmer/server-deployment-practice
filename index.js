'use strict';

/* This code is importing the `start` function from the `./src/app` module using the `require` function in Node.js. It then assigns the imported `start` function to a constant variable named `start`.
Finally, it calls the `start` function. */
const { start } = require('./src/app');

start();